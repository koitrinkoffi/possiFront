import { OralDefenseSearchPipe } from './oral-defense-search.pipe';

describe('OralDefenseSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new OralDefenseSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
