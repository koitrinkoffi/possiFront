import { LunchBreakPipe } from './lunch-break.pipe';

describe('LunchBreakPipe', () => {
  it('create an instance', () => {
    const pipe = new LunchBreakPipe();
    expect(pipe).toBeTruthy();
  });
});
