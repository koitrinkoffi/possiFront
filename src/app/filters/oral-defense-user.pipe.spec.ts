import { OralDefenseUserPipe } from './oral-defense-user.pipe';

describe('OralDefenseUserPipe', () => {
  it('create an instance', () => {
    const pipe = new OralDefenseUserPipe();
    expect(pipe).toBeTruthy();
  });
});
